
<%= form_for(@todo_list, builder: FoundationFormBuilder) do |f| %>
  <!-- Calling the custom form builder FoundationFormBuilder located in lib/foundation_form_builder.rb -->

  <%= f.text_field :title, label: "List Title", autofocus: "autofocus" %>

  <%= f.submit "Save" %>

<% end %>
